# Copiar este archivo como ".env" (sin el .example) y completar con los valores reales.
# El archivo ".env" NUNCA se sube a GitHub (ya está en .gitignore).
# Convención: todas las variables en MAYUSCULAS y no se modifican durante la ejecucion.

BOT_TOKEN=
API_KEY_DATOS_ARG=
CHAT_ID_ADMIN=
